/**
 * Shared TypeScript types for BillzCore.
 *
 * Add all shared/global type definitions here to keep the codebase DRY.
 * Feature-specific types should live alongside their feature in src/features/.
 */

export type {}; // placeholder — add shared types as the project grows
