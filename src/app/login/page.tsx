import LoginPage from "@/features/login/LoginPage";

export const metadata = {
  title: "Sign In — BillzCore",
  description:
    "Sign in to your BillzCore account to manage permits, invoices, visas, and candidates from one staffing operations ledger.",
};

export default function Page() {
  return <LoginPage />;
}
