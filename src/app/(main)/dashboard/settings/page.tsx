import SettingsPage from "@/features/settings/Settings";

export const metadata = {
  title: "Settings | BillzCore",
  description: "Manage workspace settings, team members and integrations.",
};

export default function Page() {
  return <SettingsPage />;
}
